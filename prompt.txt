I want you to implement a complete automatic APK update notification and installation system in my existing FocusShield Android app.

IMPORTANT:
Do NOT redesign unrelated parts of the app.
Do NOT remove or break existing FocusShield features.
First inspect the existing project architecture, package structure, navigation, Profile screen, notification system, permissions, Gradle configuration, and release/version configuration. Then implement this feature using the project's existing architecture and dependencies wherever possible.

GOAL:

When I publish a new FocusShield APK release on GitHub, the installed FocusShield app should automatically detect that a newer release exists and notify the user.

The update system must have THREE entry points:

1. Automatic update popup
2. Android notification
3. Profile → New Updates section with a red notification dot

-----------------------------------
1. GITHUB RELEASE UPDATE SOURCE
-----------------------------------

My GitHub repository is:

https://github.com/Aniket-Man/FOCUSSHEILD

Use GitHub Releases as the update source.

The app should check the latest published GitHub Release.

For example:

Current installed version:
1.0.0

Latest GitHub release:
1.0.1

Then the app detects:

"New update available"

The GitHub latest-release API should be used.

IMPORTANT SECURITY REQUIREMENT:

DO NOT embed a GitHub Personal Access Token, secret, or private credential inside the APK.

If the repository remains PRIVATE, do NOT implement an insecure solution by hardcoding a GitHub token into the Android application.

Design the updater so the release metadata/download URL can later be changed to a secure public endpoint/backend if necessary.

If the repository is PUBLIC, use the unauthenticated latest-release API.

Do not attempt to bypass GitHub authentication or Play Protect.

-----------------------------------
2. VERSION DETECTION
-----------------------------------

Use the Android application's actual:

versionName
versionCode

from Gradle/package metadata.

Compare the installed version against the latest GitHub release.

The system must correctly handle versions such as:

1.0.0
1.0.1
1.1.0
2.0.0

Do NOT simply compare strings alphabetically.

Prefer a robust semantic-version comparison.

The GitHub release should contain a tag such as:

v1.0.1

The updater should safely normalize:

v1.0.1 → 1.0.1

before comparison.

If the installed version is equal to or newer than the latest release:

Do nothing.

If the latest release is newer:

Mark an update as available.

-----------------------------------
3. RELEASE APK ASSET
-----------------------------------

The updater should locate the APK attached to the GitHub Release.

Expected asset example:

FocusShield-v1.0.1.apk

or

app-release.apk

Do not assume an arbitrary asset is the APK.

Prefer an asset whose filename ends with:

.apk

The update model should contain at least:

- versionName
- versionCode if available
- release tag
- release title
- release notes/body
- APK download URL
- release page URL
- published date if available

If no APK asset exists:

Do not show an installation button.

Show an appropriate message such as:

"Update is available, but the APK is not available for download yet."

-----------------------------------
4. AUTOMATIC CHECKING
-----------------------------------

Implement automatic update checking when appropriate.

Check:

- when the app starts/resumes
- when the user opens the app after a significant period
- when internet connectivity becomes available

Do NOT make GitHub requests continuously.

Avoid excessive API requests.

Use a sensible local cache/cooldown.

For example:

Do not check GitHub repeatedly every few seconds.

Store:

lastUpdateCheckTime
latestKnownVersion
updateAvailable

locally.

The exact implementation should fit the existing project architecture.

-----------------------------------
5. AUTOMATIC UPDATE NOTIFICATION
-----------------------------------

When a new release is detected, create an Android notification.

Example:

Title:
"FocusShield update available"

Body:
"FocusShield v1.0.1 is now available."

The notification should open the FocusShield update screen when tapped.

Do not create duplicate notifications every time the app starts.

Only notify when a new update is newly detected or when appropriate according to the cached state.

Use the existing notification infrastructure if the project already has one.

Respect Android notification permission requirements.

-----------------------------------
6. AUTOMATIC POPUP
-----------------------------------

When a new update is detected while the user is using FocusShield, show an update dialog/popup.

Example:

--------------------------------
FocusShield Update Available

A new version of FocusShield is available.

v1.0.1

[Release Notes]

New features...
Bug fixes...
Performance improvements...

[Download Update]
[Later]
--------------------------------

The popup must NOT appear repeatedly every time the user navigates between screens.

Once the user selects:

"Later"

the popup should be suppressed according to a sensible local state/cooldown.

However, the update must remain visible in:

Profile → New Updates

and the notification system.

The user must always be able to manually access the update later.

-----------------------------------
7. PROFILE SECTION
-----------------------------------

Modify the existing Profile screen.

Add an option:

"New Updates"

or:

"App Updates"

Use wording that fits the existing FocusShield UI.

When an update is available:

Show a small RED notification dot/badge on this profile option.

Example:

New Updates    ●

The red dot must disappear when the update has been successfully installed OR when the user explicitly marks the update as read, depending on the final architecture.

IMPORTANT:

If the user simply presses "Later", the red dot should remain because the update is still available.

-----------------------------------
8. UPDATE SCREEN
-----------------------------------

When the user taps:

Profile → New Updates

open a dedicated update screen.

Design it to match the existing FocusShield UI.

Show:

- Current version
- Latest version
- "New update available"
- Release title
- Release notes
- Published date if available
- APK download/install button

Example:

FocusShield Update

Current version
v1.0.0

Latest version
v1.0.1

What's New

• Improved focus session stability
• Fixed blocking issues
• Improved performance

[Download & Install]

-----------------------------------
9. DOWNLOAD FLOW
-----------------------------------

When the user presses:

"Download & Install"

download the APK from the release asset URL.

Use Android's proper APK installation mechanism.

Do NOT attempt to silently install or replace the application.

The Android operating system must handle the installation.

The app should correctly handle:

- download progress
- download failure
- network failure
- cancelled download
- invalid/unavailable APK
- insufficient storage
- installation permission requirements

After download completes, launch the Android package installer using the appropriate secure Android mechanism such as FileProvider/content URI.

Do NOT expose a raw file:// URI.

Do NOT use unsafe URI handling.

-----------------------------------
10. UNKNOWN APP INSTALLATION
-----------------------------------

If Android requires permission to install apps from this source:

Detect the condition.

Explain it clearly to the user.

Provide the appropriate Android system settings route where supported.

Example:

"Android needs permission to install updates downloaded from FocusShield."

[Allow Installation]

Do NOT bypass Android security restrictions.

Do NOT attempt to disable Play Protect.

Do NOT use hidden installation techniques.

The Android package installer remains responsible for installation.

-----------------------------------
11. DOWNLOAD UI
-----------------------------------

During download show a proper state:

Downloading update...

0%
25%
50%
75%
100%

Use the existing FocusShield UI components where possible.

After download:

"Download complete"

then:

"Install Update"

or automatically launch the Android installer if that is the intended UX.

Avoid downloading the same APK repeatedly.

Cache the downloaded APK safely.

Clean up obsolete APK files when appropriate.

-----------------------------------
12. RELEASE NOTES
-----------------------------------

Read release notes from the GitHub Release body.

Display them inside the update screen/dialog.

Do not display raw Markdown syntax if it can be reasonably converted into readable UI.

For example:

GitHub:

## What's New
- Improved blocking
- Fixed bugs

Display:

What's New

• Improved blocking
• Fixed bugs

Do not download arbitrary HTML and render it as trusted application UI.

-----------------------------------
13. UPDATE STATE
-----------------------------------

Create a clear update state model.

Possible states:

NoUpdate
Checking
UpdateAvailable
Downloading
Downloaded
Installing
Error

Include appropriate error messages.

The update state should survive normal screen recreation.

Do not rely only on Compose UI state.

Persist the important update metadata locally.

-----------------------------------
14. ARCHITECTURE
-----------------------------------

Follow the existing FocusShield architecture.

Prefer:

Repository
    ↓
UpdateChecker
    ↓
UpdateRepository/API
    ↓
ViewModel
    ↓
Compose UI

Do not place GitHub networking directly inside Composables.

Do not put business logic inside ProfileScreen.

Create reusable components where appropriate.

Example conceptual structure:

update/
    UpdateInfo.kt
    UpdateState.kt
    UpdateRepository.kt
    GitHubReleaseRepository.kt
    UpdateChecker.kt
    UpdateDownloadManager.kt

Adapt names/package locations to the existing project.

Do NOT create duplicate networking infrastructure if the project already has a suitable HTTP layer.

Reuse existing networking code/dependencies where possible.

-----------------------------------
15. PRIVATE GITHUB REPOSITORY
-----------------------------------

This is extremely important.

The repository may currently be PRIVATE.

DO NOT put:

GitHub PAT
GitHub password
GitHub OAuth secret
client secret

inside the APK.

Anything embedded inside an Android APK can potentially be extracted.

If the current private repository prevents secure unauthenticated release checking, implement the updater architecture in a way that allows a future secure endpoint.

For now, clearly isolate the GitHub release source behind an interface such as:

UpdateRepository

so the source can later become:

GitHub public release API
or
FocusShield backend update API

without rewriting the UI.

-----------------------------------
16. UPDATE CHECK FAILURE
-----------------------------------

If GitHub/network is unavailable:

DO NOT show an error popup every time.

The app should continue working normally.

Use cached update information when available.

Only show a meaningful error when the user manually opens the update screen or explicitly tries to check again.

Example:

"Couldn't check for updates."

[Try Again]

-----------------------------------
17. MANUAL CHECK
-----------------------------------

Inside:

Profile → New Updates

provide:

"Check for updates"

This allows the user to manually trigger a check.

Show:

"You're using the latest version."

when no update exists.

If an update exists:

show the update information.

-----------------------------------
18. RED DOT BEHAVIOR
-----------------------------------

Implement this carefully.

No update:

New Updates

Update available:

New Updates  ●

User presses Later:

New Updates  ●

User opens New Updates:

The update remains available.

Only remove the dot when:

- the update is installed successfully
OR
- the user explicitly marks the update as read, if we decide to support that behavior.

Do NOT remove the update simply because the user opened the page.

-----------------------------------
19. MULTIPLE RELEASES
-----------------------------------

Suppose the user has:

v1.0.0 installed.

GitHub has:

v1.0.1
v1.0.2
v1.1.0

The app should show the newest applicable release:

v1.1.0

It should not force the user to install every intermediate release.

Release notes should correspond to the latest available release.

-----------------------------------
20. RELEASE SECURITY / VALIDATION
-----------------------------------

Before installation, validate the downloaded file as much as practical.

At minimum:

- APK extension
- successful HTTP response
- non-empty file
- reasonable content type/response
- package/application identity where feasible
- versionCode/versionName where feasible

Do not install a file that obviously isn't an APK.

If practical, support checksum verification using a SHA-256 checksum published with the release.

If checksum verification is implemented, clearly document where the expected checksum comes from.

Do NOT invent a checksum.

-----------------------------------
21. PLAY PROTECT / ANDROID SECURITY
-----------------------------------

Do not implement anything intended to bypass:

- Google Play Protect
- Android package installation restrictions
- Android permission systems
- security warnings

FocusShield should work with Android's normal security model.

The updater should use official Android installation APIs.

-----------------------------------
22. UI QUALITY
-----------------------------------

The update UI should feel like a native part of FocusShield.

Use the existing:

- typography
- spacing
- cards
- icons
- colors
- dark/light theme
- navigation patterns

Do not create a completely unrelated visual style.

The Profile red dot should be small and polished, not a huge badge.

The update dialog should be clean and modern.

-----------------------------------
23. TESTING
-----------------------------------

After implementation, test these cases:

TEST 1:
Installed = 1.0.0
GitHub = 1.0.0

Expected:
No update.

TEST 2:
Installed = 1.0.0
GitHub = 1.0.1

Expected:
Update detected.
Red dot appears.
Notification appears.
Popup appears according to cooldown/state.

TEST 3:
User presses Later.

Expected:
Popup closes.
Red dot remains.
Update screen still shows update.

TEST 4:
User opens Profile → New Updates.

Expected:
Latest release details and release notes appear.

TEST 5:
User presses Download & Install.

Expected:
APK downloads.
Progress is shown.
Android package installer opens securely.

TEST 6:
Network unavailable.

Expected:
App continues working normally.
No repeated annoying error popups.

TEST 7:
Multiple releases exist.

Expected:
Newest release is selected.

TEST 8:
Release has no APK asset.

Expected:
Update information is shown but installation is unavailable.

TEST 9:
User opens app repeatedly.

Expected:
No duplicate notification spam.
No repeated popup spam.

TEST 10:
After successful installation.

Expected:
Old update state is cleared when the newly installed version is detected.

-----------------------------------
24. IMPORTANT VERSIONING
-----------------------------------

Make sure the existing Gradle version configuration is compatible with this updater.

For example:

versionCode = 1
versionName = "1.0.0"

Next release:

versionCode = 2
versionName = "1.0.1"

Future releases must increment versionCode.

Do not automatically modify versionCode every time the app builds.

-----------------------------------
25. FINAL IMPLEMENTATION REQUIREMENT
-----------------------------------

Before changing code:

1. Inspect the entire existing update/version configuration.
2. Inspect Profile navigation.
3. Inspect existing notification implementation.
4. Inspect existing networking architecture.
5. Inspect AndroidManifest.
6. Inspect Gradle configuration.
7. Inspect existing permission handling.
8. Identify whether the GitHub repository is currently public/private from the project's intended architecture, but do not add secrets.
9. Determine the cleanest implementation using existing dependencies.

Then implement the complete update system.

After implementation, report:

- Files created
- Files modified
- Update-check architecture
- GitHub API/release handling
- Notification behavior
- Popup behavior
- Profile red-dot behavior
- APK download/install flow
- Security considerations
- Any setup still required
- Exact steps for publishing the first GitHub release

IMPORTANT:
Do not make unrelated changes to FocusShield.
Do not remove existing functionality.
Do not hardcode secrets.
Do not bypass Android security.
Do not silently install APKs.
Build the project and fix compilation errors before considering the task complete.VERSION MANAGEMENT — IMPORTANT:

Do NOT automatically increment versionCode or versionName on every build.

The signed APK must use the versionCode and versionName defined in app/build.gradle.kts.

Version numbers are manually/release-controlled.

For example:

First release:
versionCode = 1
versionName = "1.0.0"

Second release:
versionCode = 2
versionName = "1.0.1"

Third release:
versionCode = 3
versionName = "1.1.0"

The update checker must read the actual installed application version from Android/BuildConfig and compare it with the latest GitHub Release.

Do not derive the GitHub version from the APK filename alone.

Do not change the project's version automatically during normal debug or release builds.

When the user creates a new release, they will intentionally update versionCode/versionName, build the signed APK, and upload that APK to the corresponding GitHub Release.Create the next FocusShield GitHub release.

1. Inspect the current versionCode and versionName.
2. Determine the next appropriate version.
3. Update versionCode/versionName only for this release.
4. Build the signed release APK using the existing signing configuration.
5. Verify the APK builds successfully.
6. Create the corresponding GitHub tag, for example v1.0.1.
7. Create a GitHub Release using that tag.
8. Upload the signed APK as the release asset.
9. Generate concise release notes based only on the changes actually included in this release.
10. Publish the release.

Do not expose, print, commit, or modify any signing passwords, keystores, GitHub tokens, or other secrets.
Do not create a release if the signed APK build fails.
Before publishing, show me the final versionCode, versionName, tag name, APK filename, and release notes for confirmation.